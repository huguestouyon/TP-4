window.addEventListener('DOMContentLoaded', function() {
    const cases = Array.from(document.querySelectorAll('.case'))

    let board = [
        ['','',''],
        ['','',''],
        ['','','']
    ]
    const player = 'X'
    const computer = 'O'
    let win = false
    let turn = 0

    function game() {
        if(turn % 2 === 0) {
         //function pour récupérer les coordonnées via le onclick
         // board(cord)
        }
    }
    game()
})